# Variables públicas (se envían al bundle del frontend)

# URL pública del backend (NO es proxy de Firebase)
# Ejemplo: https://api.tudominio.com/api
NEXT_PUBLIC_API_URL=

# Sentry (opcional)
NEXT_PUBLIC_SENTRY_DSN=
